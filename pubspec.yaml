
name: zimbuy
description: ZimBuy - Connecting People with Technology
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.0.0
  firebase_auth: ^4.0.0
  cloud_firestore: ^4.0.0
  google_mobile_ads: ^2.3.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
